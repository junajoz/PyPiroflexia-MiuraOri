--- a/python/Miura_Folding.py
+++ b/python/Miura_Folding.py
@@ -52,7 +52,19 @@
 
 # Call ConfigMiura to get Node and Panel arrays
 Node, Panel, BDRY = ConfigMiura(sec_hor, sec_vert, theta, a, b, fdang)
-# Node, Panel, _ = ConfigMiura(sec_hor, sec_vert, theta, a, b, fdang)
+
+# -----------------------------
+# Random nodal perturbation
+# -----------------------------
+seed = 42
+rng = np.random.default_rng(seed)
+
+perturb_scale = 0.005
+Node_pert = Node.copy()
+
+# Z_only perturbation: Add Gaussian noise to the z-coordinate of each node
+delta_z = rng.normal(loc=0.0, scale=perturb_scale, size=Node.shape[0])
+Node_pert[:, 2] += delta_z
 
 # Define material constitutive and rotational spring functions
 BarMater = lambda Ex: Ogden(Ex, E0)  # Define bar material constitutive
@@ -76,7 +88,7 @@
 
 # Perform analysis
 
-truss, angles, F = PrepareData(Node, Panel, Supp, Load, BarMater, RotSpring, Kf, Kb, Abar)
+truss, angles, F = PrepareData(Node_pert, Panel, Supp, Load, BarMater, RotSpring, Kf, Kb, Abar)
 truss['U0'] = np.zeros(3 * truss['Node'].shape[0])
 
 U_his, LF_his, Data = PathAnalysis(truss, angles, F, blam, MaxIcr)
--- a/python/PathAnalysis.py
+++ b/python/PathAnalysis.py
@@ -5,7 +5,7 @@
 from GetData import GetData
 
 from GlobalK_fast_ver import GlobalK_fast_ver
-from GlobalK_edu_ver2 import GlobalK_edu_ver2 
+#from GlobalK_edu_ver2 import GlobalK_edu_ver2
 
 from scipy.io import loadmat
 
@@ -91,7 +91,7 @@
             R[np.isnan(R)] = 0
             MRS = np.column_stack((F, R.T))
 
-            MUL[FreeDofs, :] = solve(K[np.ix_(FreeDofs, FreeDofs)].A, MRS[FreeDofs, :])
+            MUL[FreeDofs, :] = solve(K[np.ix_(FreeDofs, FreeDofs)].toarray(), MRS[FreeDofs, :])
             dUp = MUL[:, 0]
             dUr = MUL[:, 1]
             if itera == 1:
--- a/python/VisualFold.py
+++ b/python/VisualFold.py
@@ -268,18 +268,24 @@
     files = []
     Node_history = []
 
-    # Set up PyVista plotter with off_screen rendering set to False for interactive mode
-    plotter = pv.Plotter(off_screen=False, window_size=(800, 600))
+    # Set up PyVista plotter with off_screen rendering (more reliable on non-GUI/limited OpenGL systems)
+    plotter = pv.Plotter(off_screen=True, window_size=(800, 600))
     plotter.add_title("3D Truss Deformation")
     plotter.show_axes()
 
-    # Open a gif
+    # Set a stable camera orientation to avoid VTK view-up parallel warnings
+    plotter.set_viewup([0, 0, 1])
+    plotter.camera_position = 'xy'
+
+    # Open a gif (this creates the render window/context)
     plotter.open_gif("ori.gif",
         # loop=0,
         # fps=10,
         palettesize=256,
         subrectangles=False        )
 
+    # Ensure a valid render context exists before writing frames
+    plotter.render()
 
 
     mins = Node.min(axis=0)
@@ -316,7 +322,8 @@
         files.append(file)
         Node_history.append(Nodew)
 
-    plotter.show(auto_close=False)
+    # In off-screen mode we do not show an interactive window.
+    # plotter.show(auto_close=False)
 
 
     # Show the plot once all frames are created
